# {{PKG_NAME}}

A **content-automation** plugin for DeepSeek Harness (DSH), generated by [`create-dsh-content`](https://github.com/your-github-username/create-dsh-content-plugin).

## Features

- Multi-platform publishing via adapters: {{PLATFORM_LIST}}
- BYOK credential management (no platform approval needed from the plugin author)
- Cross-platform formatters: Markdown normalization, auto-split for long posts
- Structured result output with per-platform links and status

## Install

```sh
# from the parent directory of this plugin:
dsh plugin --profile my-profile add ./{{PKG_NAME}}
dsh --profile my-profile
```

## Configure

Set your platform credentials via DSH settings (they are stored in your profile config, never hardcoded):

```yaml
{{PLUGIN_ID}}:
  platforms:
    # Fill in your credentials for each platform you want to use.
    # Leave a platform out (or leave its fields empty) to disable it.
```

## Generated from

This plugin was generated by `create-dsh-content` with:
- Plugin ID: `{{PLUGIN_ID}}`
- Tool name: `{{TOOL_NAME}}`
- Platforms: `{{PLATFORM_IDS}}`
- `@deepseek-ai/dsh-tools` pinned to `{{DSH_TOOLS_VERSION}}`

{{PITFALLS}}

## License

MIT
